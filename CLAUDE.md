@AGENTS.md

<!-- SPECKIT START -->
**Active feature plan**: [`specs/001-foundation-skeleton/plan.md`](specs/001-foundation-skeleton/plan.md)

When working on this feature, read the plan first — it has the resolved technical context (`research.md`), the contract seam (`contracts/openapi.yaml`), the infrastructure entities (`data-model.md`), and the developer onramp (`quickstart.md`). The plan is the entry point; the spec (`spec.md`) is the source of truth for *what*; the plan is the source of truth for *how*.
<!-- SPECKIT END -->
